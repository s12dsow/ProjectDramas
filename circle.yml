test:
  override:
    - bin/rake spec
    - bin/rake rubocop
deployment:
  production:
    branch: master
    commands:
      - git push git@heroku.com:dramatalk.git $CIRCLE_SHA1:master
      - heroku run rake db:migrate --app dramatalk